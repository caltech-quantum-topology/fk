
utilize parallelization!!!

also incorporate q criterion (as an optional specification--if not specified, just try to bound using x)!!!

there is an issue with the degree; the program always computes lower than the specified degree!!!

add link computations!!!!!

check qalg edge cases!!!!!!

maybe fk.cpp (base) doesn't work for some knots because what I previously thought were trivial inequalities are, in fact, not, and they are pathologically being omitted.

sort out singular change-of-basis and redundancy in C++ angles program

Change fibered_knots.csv to knotinfo_formatted.csv, which contains all the knots Knotinfo has with their corresponding braids. Make sure to likewise change the README file.

See if summing FK like ADO makes it converge.

Polynomial Time Computation for FK by Inverting Matrices and taking determinant explicitly

Exploring FK in terms of Hook Tableaux

See if you can get the remaining 12 crossing sign diagrams by increasing the degree of the search.

Do we have proofs of anything slopes related for homogeneous braid knots?

Don't forget to implement coefficient factors such as (-1)^s for FK

use custom numerics for state values (only need 1/2 and signs at most--a byte is a good compromise)

Make this program into a general state sum computational library (see the DeepSeek conversation for insight into solid software architecture setup)
The general state sum library should have files for the following features:
    R-matrices, tests in post (with testing and production modes for efficiency in the latter), summation conventions, sign diagram (or other precondition-type things) searches,
    what else?

Make a general state sum library that can be used for other quantum invariants (e.g. ADO, colored Jones, etc.)

Change the solution pool to be more general
